//! > TODO(yg)

//! > test_runner_name
test_function_lowering

//! > function
fn foo2() {
  foo()
}

//! > function_name
foo2

//! > module_code
extern fn bar() implicits(RangeCheck) nopanic;
fn foo() implicits() {
  bar()
}

//! > semantic_diagnostics

//! > lowering_diagnostics

//! > lowering_flat
Parameters: v1: core::RangeCheck
blk0 (root):
Statements:
  (v2: core::RangeCheck, v0: ()) <- test::foo(v1)
End:
  Return(v2, v0)
